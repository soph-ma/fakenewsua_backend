# Fake News Detector for Ukrainian Language - BACKEND 

This server involves FastAPI and uvicorn to create and run the server, and torch for loading the model.

to run: 
`pip install -r requirements.txt`
`python server.py`
